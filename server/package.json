{
  "name": "server",
  "type": "module",
  "scripts": {
    "generate": "bun run scripts/generate-coffee-data.ts",
    "dev": "bun run generate && wrangler dev",
    "deploy": "bun run generate && wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.11.9",
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}