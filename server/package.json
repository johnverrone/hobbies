{
  "name": "server",
  "type": "module",
  "scripts": {
    "generate": "bun run scripts/generate-coffee-data.ts",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.2.3",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "agents": "^0.5.0",
    "hono": "^4.12.0",
    "octokit": "^5.0.5",
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260227.0",
    "wrangler": "^4.67.0"
  }
}