{
  "name": "server",
  "type": "module",
  "scripts": {
    "generate": "bun run scripts/generate-coffee-data.ts && bun run scripts/generate-guitar-journal.ts",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "hono": "^4.12.0",
    "octokit": "^5.0.5",
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "wrangler": "^4.67.0"
  }
}